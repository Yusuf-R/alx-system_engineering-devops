#!/usr/bin/env bash
# resolving the config of the nginx server to return curl request on port80
sudo ln -s /etc/nginx/sites-available/default /etc/nginx/sites-enabled/default
sudo service nginx restart
pkill -o nginx
